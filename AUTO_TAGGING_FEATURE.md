# Auto Tagging for AI-Generated Code

## Overview

The Auto Tagging feature in ShadowPilot automatically adds identifying tags to code generated by GitHub Copilot. This helps teams track, audit, and manage AI-generated code within their codebase.

When enabled, ShadowPilot monitors GitHub Copilot operations and automatically adds tags when:
- **Inline suggestions** are accepted (via Tab key)
- **Copilot Edits** are applied (via Keep button for entire file generation)

Tags are added based on the code structure:
- **Multi-line code**: Start and end tags wrap the generated code block
- **Single-line code**: A single tag is placed above the line

## Enabling Auto Tagging

1. Go to **Tools → Options → Shadow Pilot → General**
2. Find the **Auto Tagging** section
3. Set **Enable Auto Tagging** to `True`
4. Click **OK** to save

Once enabled, all subsequent GitHub Copilot code acceptances will be automatically tagged.

## Configuration Options

You can customize the tag text in **Tools → Options → Shadow Pilot → General**:

| Option | Default Value | Description |
|--------|---------------|-------------|
| **Enable Auto Tagging** | `False` | Enable or disable automatic tagging of AI-generated code |
| **Start Tag** | `// [AI-GENERATED CODE START]` | Tag placed before multi-line AI-generated code |
| **End Tag** | `// [AI-GENERATED CODE END]` | Tag placed after multi-line AI-generated code |
| **Single Line Tag** | `// [AI-GENERATED]` | Tag placed above single-line AI-generated code |

## Supported Scenarios

### 1. Inline Suggestions (Tab to Accept)

When you type code and Copilot shows an inline suggestion:
1. Press **Tab** to accept the suggestion
2. Tags are automatically added around the inserted code

### 2. Copilot Edits / Chat (Keep Button)

When Copilot generates entire file content or significant changes:
1. Review the generated code in the diff view
2. Click **Keep** to accept the changes
3. Tags are automatically added around the entire generated content

## Examples

**Example - Multi-line code:**
```csharp
// [AI-GENERATED CODE START]
public void ProcessData(string input)
{
    if (string.IsNullOrEmpty(input))
        throw new ArgumentNullException(nameof(input));
    
    var result = input.Trim().ToUpper();
    Console.WriteLine(result);
}
// [AI-GENERATED CODE END]
```

**Example - Single-line code:**
```csharp
// [AI-GENERATED]
var items = collection.Where(x => x.IsActive).OrderBy(x => x.Name).ToList();
```

**Example - Entire file generated by Copilot Edits:**
```csharp
// [AI-GENERATED CODE START]
using System;
using System.Collections.Generic;

namespace MyApp
{
    public class GeneratedService
    {
        public void Execute()
        {
            // Implementation
        }
    }
}
// [AI-GENERATED CODE END]
```

## Language Support

The tagging feature automatically detects the programming language and uses the appropriate comment style:

| Language | Comment Style |
|----------|---------------|
| C#, C++, Java, JavaScript, TypeScript | `//` |
| Python, Ruby, Perl, PowerShell, Bash | `#` |
| SQL | `--` |
| VB.NET | `'` |
| HTML, XML, XAML | `<!-- -->` |
| CSS, LESS, SCSS | `/* */` |

## How It Works

### Detection Method

ShadowPilot monitors Visual Studio command execution to detect Copilot operations:

1. **Command Interception**: Monitors `CommandEvents.BeforeExecute` and `AfterExecute`
2. **Copilot Commands**: Detects GitHub Copilot command set (`{39B0DEDE-D931-4A92-9AA2-3447BC4998DC}`)
3. **Tab Key Acceptance**: Monitors Tab key in text editors for inline suggestions
4. **File-Level Changes**: Detects commands with ID >= 256 (Keep, Accept All, Apply)

### Tag Placement

Tags are placed using line-based positioning:
- **Start tag**: Inserted at the beginning of the first line of generated code
- **End tag**: Inserted at the end of the last line of generated code (for multi-line)
- **Single-line tag**: Inserted at the beginning of the line containing the code

## Best Practices

1. **Enable during development**: Turn on auto-tagging when actively using Copilot
2. **Review tagged code**: Use tags to identify code that may need extra review
3. **Customize tags**: Adjust tag text to match your team's conventions
4. **Code reviews**: Use tags to quickly identify AI-generated sections during reviews
5. **Compliance**: Tags help with auditing and compliance requirements for AI-generated code

## Troubleshooting

### Tags not appearing

- Verify auto-tagging is enabled in **Tools → Options → Shadow Pilot → General**
- Ensure you're accepting Copilot suggestions (pressing Tab when suggestion is visible)
- For Copilot Edits, make sure you click "Keep" to accept the changes
- Check the VS Output window (Debug) for diagnostic messages

### Tags in wrong position

- The tagging uses line-based positioning for accuracy
- If tags appear in unexpected positions, try reapplying the Copilot suggestion
- Ensure the document is not being modified by other extensions simultaneously

### Wrong comment style

- The extension detects language from the file's content type
- For unsupported languages, C-style comments (`//`) are used by default

## Version History

- **v5.1**: Added Auto Tagging feature for AI-generated code
  - Command-based detection of Copilot suggestion acceptance
  - Support for Copilot Edits (Keep button) file-level generation
  - Line-based tag positioning for accurate placement
  - Intelligent filtering to reduce false positives
  - Customizable tags for multi-line and single-line code
  - Language-aware comment style detection
